# This file is for enabling/disabling bot features

extensions:
  - extension: owner
  - extension: test
  - extension: utility
  - extension: virtual_profiles
  - extension: virtual_roles
  - extension: virtual_reaction_roles
  - extension: virtual_reaction_handler

commands:
  - command: test
    enabled: true